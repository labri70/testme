# 커서(Cursor) 토큰 변환 작업 가이드

이 문서는 Figma Token Studio에서 내려받은 디자인 토큰을 CSS, SCSS, JS 파일로 변환하는 전체 프로세스를 설명합니다.

---

## 📁 프로젝트 구조

```
testme/
├── tokens/                          # 토큰 소스 파일
│   ├── figma-token.json            # Figma Token Studio에서 내려받은 원본 토큰
│   └── sd-input/                   # Style Dictionary 입력용 변환된 토큰
│       └── tokens.json             # Token Transformer로 변환된 토큰 (자동 생성)
│
├── css/                            # CSS 출력 파일
│   ├── variables.css               # CSS 변수 파일 (자동 생성)
│   └── main.css                    # SCSS 컴파일 결과물 (자동 생성)
│
├── scss/                           # SCSS 소스 파일
│   ├── _variables.scss             # SCSS 변수 파일 (자동 생성)
│   └── main.scss                   # 메인 SCSS 파일 (직접 작성)
│
├── js/                             # JavaScript 출력 파일
│   └── tokens.js                   # JS 토큰 파일 (자동 생성)
│
├── config.json                     # Style Dictionary 설정 파일
├── fix-tokens.js                   # 토큰 후처리 스크립트
├── package.json                    # 프로젝트 의존성 및 스크립트
├── index.html                      # HTML 예제 파일
└── README.md                       # 프로젝트 설명서
```

---

## 🚀 설치 및 초기 설정

### 1. Node.js 설치 확인
```bash
node --version
npm --version
```

### 2. 의존성 설치
```bash
npm install
```

설치되는 패키지:
- `style-dictionary`: 디자인 토큰을 CSS/SCSS/JS로 변환
- `token-transformer`: Figma 토큰을 변환
- `sass`: SCSS를 CSS로 컴파일

---

## 📝 주요 파일 설명

### `tokens/figma-token.json`
- **용도**: Figma Token Studio에서 GitHub로 업로드한 원본 토큰 파일
- **위치**: GitHub에서 클론 또는 pull로 가져옴
- **특징**: 
  - 원본 파일이므로 직접 수정하지 않음
  - 참조 형식 사용: `{blue.500}`, `{Colors/Mode 1.blue.500}` 등
  - 토큰 세트 구조 유지: `Colors/Mode 1`, `Brand/Mode 1` 등

### `config.json`
- **용도**: Style Dictionary 빌드 설정
- **주요 설정**:
  - `source`: `tokens/sd-input/*.json` (변환된 토큰 파일 경로)
  - `platforms`: CSS, SCSS, JS 플랫폼 설정
  - `outputReferences: true`: 토큰 참조를 유지

### `fix-tokens.js`
- **용도**: Token Transformer 출력 후처리 스크립트
- **주요 기능**:
  1. **경로 정규화**: 불완전한 참조를 전체 경로로 변환
  2. **구조 단순화**: 평탄화된 토큰 구조 유지 (CSS 변수명을 짧게 유지)
  3. **데이터 무결성**: 원본 파일(`figma-token.json`)은 수정하지 않음
  4. **메타데이터 제거**: `tokenSetOrder` 등 불필요한 메타데이터 제거

### `package.json`
- **스크립트**:
  - `npm run transform`: Token Transformer 실행
  - `npm run fix-tokens`: 토큰 후처리 실행
  - `npm run build`: Style Dictionary 빌드 실행
  - `npm run build:tokens`: 전체 토큰 변환 프로세스 실행

---

## 🔄 토큰 변환 워크플로우

### 전체 프로세스 개요

```
Figma Token Studio
    ↓ (GitHub 업로드)
GitHub Repository
    ↓ (Git pull/clone)
tokens/figma-token.json
    ↓ (npm run transform)
tokens/sd-input/tokens.json (Token Transformer : 토큰 변환)
    ↓ (npm run fix-tokens)
tokens/sd-input/tokens.json (후처리 완료)
    ↓ (npm run build) (Style Dictionary 빌드 : 디바이스 포맷별 최종 결과물생성)
css/variables.css + scss/_variables.scss + js/tokens.js
```

### 단계별 상세 설명

#### 1단계: 토큰 변환 (Token Transformer)
```bash
npm run transform
```

**작업 내용**:
- `tokens/figma-token.json`을 읽어서 평탄화(flatten) 처리
- 참조 해결: `{blue.500}` → `#3b82f6`
- `tokens/sd-input/tokens.json`으로 출력

**결과 예시**:
```json
{
  "primary": {
    "value": "#3b82f6",
    "type": "color"
  },
  "blue": {
    "50": {
      "value": "#eff6ff",
      "type": "color"
    }
  }
}
```

#### 2단계: 토큰 후처리 (fix-tokens.js)
```bash
npm run fix-tokens
```

**작업 내용**:
- 해결되지 않은 참조 제거
- 메타데이터 제거 (`tokenSetOrder`, `$metadata` 등)
- 평탄화된 구조 유지 (CSS 변수명을 짧게 유지)

**결과**:
- 긴 이름 제거: `--colors-mode-1-blue-50` ❌
- 짧은 이름 유지: `--blue-50` ✅

#### 3단계: Style Dictionary 빌드
```bash
npm run build
```

**작업 내용**:
- `tokens/sd-input/tokens.json`을 읽어서 각 플랫폼으로 변환
- CSS: `css/variables.css` (CSS 변수)
- SCSS: `scss/_variables.scss` (SCSS 변수)
- JS: `js/tokens.js` (ES6 모듈)

---

## 🛠️ 전체 빌드 실행

### 방법 1: 단계별 실행
```bash
npm run transform      # Token Transformer 실행
npm run fix-tokens     # 토큰 후처리
npm run build          # Style Dictionary 빌드
```

### 방법 2: 한 번에 실행 (권장)
```bash
npm run build:tokens
```

이 명령어는 위의 3단계를 순서대로 자동 실행합니다.

---

## 📊 출력 파일 예시

### CSS 변수 (`css/variables.css`)
```css
:root {
  --primary: #3b82f6;
  --secondary: #f97316;
  --blue-50: #eff6ff;
  --blue-100: #dbeafe;
  /* ... */
}
```

### SCSS 변수 (`scss/_variables.scss`)
```scss
$primary: #3b82f6;
$secondary: #f97316;
$blue-50: #eff6ff;
$blue-100: #dbeafe;
/* ... */
```

### JavaScript (`js/tokens.js`)
```javascript
export const Primary = "#3b82f6";
export const Secondary = "#f97316";
export const Blue50 = "#eff6ff";
export const Blue100 = "#dbeafe";
/* ... */
```

---

## 💡 사용 예시

### HTML에서 CSS 변수 사용
```html
<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="css/variables.css">
  <style>
    .button {
      background-color: var(--primary);
      color: white;
    }
  </style>
</head>
<body>
  <button class="button">Button</button>
</body>
</html>
```

### SCSS에서 변수 사용
```scss
@import "./_variables.scss";

.button {
  background-color: $primary;
  color: white;
}

.button-secondary {
  background-color: $secondary;
}
```

SCSS 컴파일: 
```json
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transform": "token-transformer tokens/figma-token.json tokens/sd-input/tokens.json",
    "fix-tokens": "node fix-tokens.js",
    "build": "style-dictionary build --config config.json",
    "build:scss": "sass --watch scss/main.scss css/main.css",
    "build:tokens": "npm run transform && npm run fix-tokens && npm run build",
    "build:all": "npm run build:tokens && npm run build:scss"
  },
```
or
Live Sass Compiler 설치 후 Watch 실행 후 브라우저로 확인


### JavaScript에서 토큰 사용
```javascript
import { Primary, Blue500 } from './js/tokens.js';

console.log(Primary); // "#3b82f6"
```

---

## 🔄 GitHub 연동 워크플로우

### 1. Figma → GitHub (토큰 업로드)
1. Figma Token Studio에서 토큰 편집
2. GitHub로 토큰 업로드
3. `tokens/figma-token.json` 파일 업데이트

### 2. GitHub → Cursor (토큰 다운로드)
```bash
git pull origin main
# 또는
git clone <repository-url>
```

### 3. Cursor에서 변환 작업
```bash
npm run build:tokens
```

### 4. Cursor에서 토큰 수정 (필요시)
- `tokens/figma-token.json` 파일 직접 수정
- 또는 새로운 토큰 값으로 업데이트

**예시**: `Brand/Mode 1.primary.value`를 `{#4f46e5}`로 변경

### 5. 변환 및 빌드
```bash
npm run build:tokens
```

### 6. Cursor → GitHub (변경사항 업로드)
```bash
git add tokens/figma-token.json
git commit -m "Update primary color"
git push origin main
```

### 7. GitHub → Figma (변경사항 동기화)
1. Figma Token Studio에서 GitHub 연결 확인
2. GitHub에서 변경사항 받아오기
3. Figma에 변경된 토큰 반영

---

## ⚠️ 주의사항

### 원본 파일 보호
- **`tokens/figma-token.json`**: 원본 파일이므로 `fix-tokens.js`가 수정하지 않음
- **`tokens/sd-input/tokens.json`**: 자동 생성 파일이므로 직접 수정하지 말 것
- **출력 파일들** (`css/`, `scss/_variables.scss`, `js/tokens.js`): 모두 자동 생성 파일

### 파일 수정 권장 사항
- ✅ **수정 가능**: `tokens/figma-token.json`, `scss/main.scss`, `index.html`
- ❌ **수정 금지**: `tokens/sd-input/tokens.json`, `css/variables.css`, `scss/_variables.scss`, `js/tokens.js`

### 토큰 수정 방법
1. `tokens/figma-token.json` 파일을 직접 편집
2. `npm run build:tokens` 실행하여 다시 빌드
3. 변경사항이 모든 출력 파일에 반영됨

---

## 🐛 문제 해결

### 문제 1: "No tokens found" 오류
**원인**: `tokens/sd-input/tokens.json` 파일이 없거나 비어있음
**해결**: 
```bash
npm run transform
npm run fix-tokens
```

### 문제 2: 참조 오류 (예: `{blue.500}` 해결 불가)
**원인**: Token Transformer가 참조를 해결하지 못함
**해결**: `fix-tokens.js`가 자동으로 처리하지만, 원본 파일의 참조 형식 확인 필요

### 문제 3: 긴 변수명이 생성됨 (예: `--colors-mode-1-blue-50`)
**원인**: `fix-tokens.js`가 실행되지 않음
**해결**: `npm run build:tokens` 전체 프로세스 실행 확인

### 문제 4: SCSS 컴파일 오류
**원인**: `_variables.scss` 파일이 없음
**해결**: 
```bash
npm run build:tokens  # Style Dictionary 빌드로 _variables.scss 생성
sass scss/main.scss css/main.css
```

---

## 📚 추가 리소스

- [Style Dictionary 공식 문서](https://styledictionary.com/)
- [Token Transformer GitHub](https://github.com/six7/figma-token-transformer)
- [Figma Token Studio](https://www.figma.com/community/plugin/888356646278934516/Token-Studio)

---

## 📝 체크리스트

토큰 변환 작업을 수행하기 전 확인:

- [ ] Node.js 및 npm 설치 확인
- [ ] `npm install` 실행 완료
- [ ] `tokens/figma-token.json` 파일 존재 확인
- [ ] `npm run build:tokens` 실행 성공
- [ ] 출력 파일 생성 확인 (`css/variables.css`, `scss/_variables.scss`, `js/tokens.js`)
- [ ] 브라우저에서 CSS 변수 적용 확인

---

**마지막 업데이트**: 2024년 1월 2일

